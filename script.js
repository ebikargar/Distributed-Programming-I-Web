    function foo() {
        alert("Your reservation done successfully!");
        return true;
    }
    function ConfirmDelete()
    {
      var x = confirm("Are you sure you want to delete?");
      if (x)
          return true;
      else
        return false;
    }
	
	/*function confirmpar()
	{
	var x, text;

	// Get the value of the input field with id="numb"
	x = document.getElementById("pnumber").value;

	// If x is Not a Number or less than 1 or greater than 10, output "input is not valid"
	// If x is a number between 1 and 10, output "Input OK"

	if (isNaN(x) || x > 100) {
		text = "Input not valid";
	} else {
		text = "Input OK";
	}
}
//function to check userid & password
function check(form)
{
 /*the following code checkes whether the entered userid and password are matching*/
 //if(form.username.value == "myuserid" && form.password.value == "mypswrd")
  //{
    //window.open('register.php')/*opens the target page while Id & password matches*/
  //}
 //else
 //{
   //alert("Error Password or Username")/*displays error message*/
  //}
//}*/



